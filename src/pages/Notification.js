import Notification from "@/components/Layout/Notification";
import React, { Fragment, useEffect } from "react";
const NotificationPage = ({ email }) => {
    return (
        <Fragment>
            <Notification />
        </Fragment>
    );
};

export default NotificationPage;

